import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  direction: "rtl",
  palette: {
    primary: {
      main: "#199DA3",
    },
    secondary: {
      main: "#7E8299",
    },
  },
  typography: {
    fontFamily: "vazir",
  },
});

export default theme;
